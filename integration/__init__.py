"""Layer-neutral post-run / publish integrations (TestRail, CQM).

This package holds the integration code that any automation layer can call
after a run, plus the standalone manual-case publisher under ``testrail/``.
"""
