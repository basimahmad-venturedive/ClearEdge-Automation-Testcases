# automation/api-ts/envs/.env.prod.example  <- commit this; copy to .env.prod and fill in
# Target: production clearedge-backend. NOT YET PROVISIONED (2026-07-07) — placeholders only.
#
# Per PROJECT_TEST_PLAN.md §3.1 (governed environment usage): QA runs exclusively on staging/QA.
# Production credentials should exist here ONLY for a deliberate, approved smoke-test pass —
# never for routine automated runs. Treat any real values placed here with production-secret
# handling (rotate immediately after a one-off smoke test).

API_BASE_URL=https://api.clearedgeiq.example/api/v1
HEALTH_BASE_URL=https://api.clearedgeiq.example

COGNITO_TENANT_USER_POOL_ID=
COGNITO_TENANT_APP_CLIENT_ID=
COGNITO_ADMIN_USER_POOL_ID=
COGNITO_ADMIN_APP_CLIENT_ID=
AWS_REGION=

TEST_DATABASE_URL=
REDIS_HOST=
REDIS_PORT=6379

MAX_RESPONSE_TIME_S=3.0
USE_MOCK=0
